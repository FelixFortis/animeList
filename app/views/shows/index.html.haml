%h1 Listing shows

%table
  %tr
    %th Name
    %th Description
    %th Total episodes
    %th Image url
    %th
    %th
    %th

  - @shows.each do |show|
    %tr
      %td= show.name
      %td= show.description
      %td= show.total_episodes
      %td= show.image_url
      %td= link_to 'Show', show
      %td= link_to 'Edit', edit_show_path(show)
      %td= link_to 'Destroy', show, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Show', new_show_path
